# Core requirements (automatically installed with pip install)
jupyterlab>=4.0
jupyter-server>=2.0
python-dotenv>=1.0

# Optional: LLM providers — install the one(s) you use
# pip install varys[anthropic]
anthropic

# pip install varys[openai]
# openai

# pip install varys[ollama]
# httpx

# pip install varys[rag]
# chromadb
